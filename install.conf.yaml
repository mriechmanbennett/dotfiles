- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.vimrc:
    ~/.zshrc:
    ~/.gitignore_global:
    ~/.gitconfig:

- shell:
  - [git submodule update --init --recursive, Installing submodules]
